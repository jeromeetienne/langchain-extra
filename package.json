{
	"name": "langchain-extra",
	"version": "1.0.2",
	"description": "",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"clean": "rm -rf ./dist",
		"bump_version": "npm version patch -m \"Bump version to %s\"",
		"bump_publish": "npm version patch && git push --follow-tags && npm run npm_publish",
		"npm_publish": "npm run build && npm publish --access public",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/jeromeetienne/langchain-extra.git"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/jeromeetienne/langchain-extra/issues"
	},
	"homepage": "https://github.com/jeromeetienne/langchain-extra#readme",
	"dependencies": {
		"@langchain/core": "^1.0.2",
		"langchain": "^1.0.2",
		"langchain-extra": "^1.0.0"
	},
	"devDependencies": {
		"@langchain/google-genai": "^1.0.0",
		"@types/node": "^24.9.2",
		"typescript": "^5.9.3"
	}
}